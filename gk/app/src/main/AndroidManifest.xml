<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quyền kết nối Internet để ứng dụng gọi được Web API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gk"
        android:usesCleartextTraffic="true">

        <!-- Màn hình chính: hiển thị danh sách đơn hàng -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Gk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Màn hình thêm đơn hàng mới -->
        <activity
            android:name=".AddNewActivity"
            android:exported="false"
            android:label="Thêm Đơn Hàng"
            android:theme="@style/Theme.Gk" />

        <!-- Màn hình cập nhật và xóa đơn hàng -->
        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:label="Cập nhật Đơn Hàng"
            android:theme="@style/Theme.Gk" />

    </application>

</manifest>